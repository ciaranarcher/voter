<h3 id="topic" data-key="<%= @topic.key %>">Vote on Topic '<%=@topic.name %>'</h3>

<p>
  Hi, <span id="participant_name"><%= @participant_name %></span> 
  (<span id="participant_email"><%= @participant_email %></span>). 
  Please vote on an option below...
</p>

<ul id="vote_options">
  <% @vote_options.each do |option| %>
    <li>
      <%= option.name %>
      <input type="button" data-id="<%= option._id %>" data-value="<%= option.name %>" value="Vote" />
    </li>
  <% end %>
</ul>